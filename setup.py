from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='To create MLOps pipeline for neural machine learning project using tool such as mlflow, dvc etc',
    author='Ankush',
    license='',
)
